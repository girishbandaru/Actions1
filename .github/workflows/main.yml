#first github actions file

name: simple echo

on: 
  push:
    branches:
     - main
jobs:
  echo_job:
    name: Echo message
    runs-on: ubuntu-latest

    steps:
    - name: Echo hello
      run: echo "Hello, world"
  
